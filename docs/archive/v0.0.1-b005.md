# v0.0.1-b005

Version : v0.0.1-b005
Date    : 2026-06-03
Status  : Alpha

## Changes

S : Documented 10 stop-ship security findings — full report at `docs/SECURITY-AUDIT.md`
S : F-MEM-1 (Critical) — f-string `{...}` placeholder content pasted verbatim into emitted C → RCE primitive
S : F-TY-2 (Critical) — `Result<T,E>` / `Option<T>` payload truncated to `int64_t`
S : F-COMP-1 (High) — parser recursion unbounded → stack-overflow DoS via `*****T`
S : F-COMP-2 (High) — `let mut` mutability not enforced by sema
S : F-COMP-3 (High) — `match` lacks exhaustiveness checking
S : F-MEM-2 (High) — `lex_string` realloc result not NULL-checked
S : F-MEM-5 (High) — `read_file` does not zero the tail on short reads; no file-type check
S : F-MEM-7 (High) — `urus_print_fmt` walks args[] until sentinel with no length bound
S : F-MEM-8 (High) — `URUS_FMT_PTR_TAG` allows pointer-as-string type confusion
S : F-TY-1 (High) — `Result` / `Option` share tag namespace; cross-cast = type confusion
A : `tests/fail/SEC-01_fstr_injection.urus` — regression PoC for F-MEM-1
A : 14-phase audit document with scorecard and TOP 100 hardening list (Tier 0 → Tier 4)
N : Overall security score 2.5/10. Do not advertise "safer than C" until F-MEM-1 lands.
N : v0.0.2 is defined as the security release that closes all 10 stop-ship findings

## Linked work

- docs/SECURITY-AUDIT.md (later moved to docs/security/ in b006)
- tests/fail/SEC-01_fstr_injection.urus
- C:/Users/rasya/.claude/projects/D--Urus/memory/urus-v0-0-1-security-stopship.md
