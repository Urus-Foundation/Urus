# v0.0.1-b027

Version : v0.0.1-b027
Date    : 2026-06-04
Status  : Alpha

## Changes

U : **`06-LANGUAGE-GUIDE.md` brought up to date with b013–b025 reality.**  Three stale danger-notes were *outdated in the good direction* — the guide still warned about holes that have been closed for 14 builds:
    - "mut not yet enforced (F-COMP-2)" → replaced with the actual enforcement rules (assignment, compound assignment, `&mut` borrow, unused-`mut` warning).
    - "exhaustiveness not checked, be paranoid (F-COMP-3)" → replaced with the checked rules (Result / Option / user enums, wildcard escape, guarded arms don't count).
    - "f-string pastes `{...}` into C — RCE, do not compile untrusted source (F-MEM-1)" → replaced with the validator's accept/reject table.
U : Guide additions: `while let` under Control flow (with desugar note), `for` both-bounds requirement, `?` context rule (b018), new *Standard library* section (println family, panic, read_line, the seven `urus_str_*` helpers), refreshed *What is missing* (16-byte payload cap note, defer-on-return).
A : `examples/echo.urus` — interactive echo loop: `read_line` + `while let` + `urus_str_eq` + f-strings + `let mut` counter.
A : `examples/grades.urus` — enum + exhaustive match over user enum + Result + for-range inclusive, the full sema surface in ~50 lines.
N : Examples feed the fuzz corpus automatically (`make-corpus.sh` globs `examples/*.urus`), so both new files also widen fuzz coverage seeds.
N : Guide footer now carries the build stamp (`Last updated 2026-06-04 (v0.0.1-b027)`).

## Linked work

- docs/documentation/06-LANGUAGE-GUIDE.md
- examples/echo.urus
- examples/grades.urus
